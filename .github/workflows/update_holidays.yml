name: Update Holidays JSON

on:
  schedule:
    # 毎月1日の 日本時間 朝9:00 (UTC 0:00) に自動実行
    - cron: '0 0 1 * *'
  workflow_dispatch: # 手動実行ボタンも残しておく

permissions:
  contents: write # ファイル保存のために書き込み権限が必要

jobs:
  update-json:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Dart
        uses: dart-lang/setup-dart@v1
        
      - name: Install dependencies
        run: dart pub get

      - name: Run generation script (Holidays JP)
        # APIキーが不要になったので、envセクションは削除しました
        run: dart test/generate_holidays.dart

      - name: Commit and Push if changed
        run: |
          git config --global user.name 'Holiday Updater Bot'
          git config --global user.email 'action@github.com'
          git add holidays.json
          # 変更がある場合のみコミット＆プッシュ（エラー回避）
          git diff --quiet && git diff --staged --quiet || (git commit -m "📅 Update holiday data from Holidays JP" && git push)